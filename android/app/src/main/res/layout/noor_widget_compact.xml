<?xml version="1.0" encoding="utf-8"?>
<!--
  Quick Dhikr Tally Widget (2×1) — the slim, single-row sibling of the
  full Tasbeeh counter (2×2). No target/reset/next controls, no ring —
  the whole tile is one tap-to-increment zone showing today's rotating
  dhikr phrase and a live tally. Gets the same living sky + starfield +
  selectable theme as every other widget. See NoorCompactWidgetProvider.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/noor_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/noor_widget_background">

    <ImageView
        android:id="@+id/compact_sky"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:importantForAccessibility="no" />

    <ImageView
        android:id="@+id/compact_stars"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:importantForAccessibility="no" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="11dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center_vertical|end"
            android:orientation="vertical">

            <TextView
                android:id="@+id/noor_widget_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:gravity="end"
                android:maxLines="1"
                android:textColor="@color/widget_text_accent"
                android:textSize="10sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/noor_widget_phrase"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:ellipsize="end"
                android:gravity="end"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:textColor="@color/widget_text_primary"
                android:textSize="15sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Live tally badge — noor_widget_pill is a fixed gold-gradient brand
             background with no night variant, so its text stays fixed dark. -->
        <TextView
            android:id="@+id/compact_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:background="@drawable/noor_widget_pill"
            android:minWidth="28dp"
            android:gravity="center"
            android:paddingHorizontal="9dp"
            android:paddingVertical="5dp"
            android:text="0"
            android:textColor="#2F4F37"
            android:textSize="14sp"
            android:textStyle="bold" />
    </LinearLayout>
</FrameLayout>
